# Kubernetes 1.24+ no longer auto-creates tokens for ServiceAccounts.
# This Secret explicitly requests a long-lived token bound to jenkins-sa.
apiVersion: v1
kind: Secret
metadata:
  name: jenkins-sa-token
  namespace: testing
  annotations:
    kubernetes.io/service-account.name: jenkins-sa
type: kubernetes.io/service-account-token